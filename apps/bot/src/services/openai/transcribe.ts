/**
 * Notas de voz (Fase 3).
 *
 * Whisper sobre el audio descargado. El texto transcrito entra al mismo flujo
 * que un mensaje escrito, así que el resto del motor no sabe si el cliente
 * habló o escribió.
 */
export {};
