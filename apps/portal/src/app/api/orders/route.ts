/**
 * Pedidos del portal (Fase 5).
 *
 * GET: el tablero (todo menos los borradores — un carrito que nadie cerró no
 * es un pedido).
 * POST: cambio de estado. El aviso al cliente va al OUTBOX, no directo a
 * WhatsApp: que Meta esté caída no puede bloquear la cocina.
 */
export {};
